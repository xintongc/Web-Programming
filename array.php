$original = arvag{"a","b","c".......};
$unique = arragy();
for($x = 0; $x < count($original); $x++){
	if(array_key_exists($original[$x],$unique))
		$unique[$orinial[$x]]++; //if the letter is there, then add 1 to the value. if the letter in not there, then create a new one and assign 1.
	else
		$unique[$original[$]]  //the index is the new things and the number is 1
}

